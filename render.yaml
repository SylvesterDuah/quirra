# render.yaml
services:
  - type: web
    name: quirra-api
    env: python
    plan: free
    region: oregon
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput || true
    # Run migrations first, then launch gunicorn
    startCommand: bash -lc "python manage.py migrate --noinput && gunicorn quirra.wsgi:application --bind 0.0.0.0:$PORT"
    healthCheckPath: /health/              # matches quirra/urls.py
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: QUIRRA_USER_SALT
        generateValue: true
      - key: DEBUG
        value: "0"
      - key: DATABASE_URL
        fromDatabase:
          name: quirra-db
          property: connectionString
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "1"
      # tighten later:
      # - key: CORS_ALLOWED_ORIGINS
      #   value: https://your-frontend.vercel.app
      # - key: CSRF_TRUSTED_ORIGINS
      #   value: https://your-frontend.vercel.app
      # - key: INGEST_SECRET
      #   value: paste-a-long-random-secret

databases:
  - name: quirra-db
    plan: free
    postgresMajorVersion: 15
    ipAllowList: []
